<div class="container mt-5">
    <div class="row text-center mt-4 d-flex justify-content-around">
        <div class="col-md-3">
            <div class="card mb-5 shadow-lg p-3 mb-5 bg-body rounded" style="width: 18rem;">
                <img src="{{imageUrl}}" class="card-img-top" alt="photo">
                <div class="card-body">
                    <h2 class="card-title">{{name}}</h2>
                    <hr>
                    <h4>{{eventName}}</h4>
                    <p class="card-text">{{description}}</p>
                    <p>{{date}}</p>

                    <a href="/comentarios/crear/{{id}}">Crear comentario</a>
                    <br>

                </div>
            </div>
        </div>
        <div class="col-md-3">
            {{!-- {{#each userComments}} --}}
            <div class="card shadow-lg p-3 mb-5 bg-body rounded">
                <div class="card-header">
                    <h5>{{name}}</h5>
                </div>
                <div class="card-body">
                    <blockquote class="blockquote mb-0">
                        <p>{{description}}dshflkwhfklawdhfliawhjflishadlfkashdklfhsdlkfhsldhfaslkdhlsakdhflsdkfhslkdfhaskljdhfslkfhsdkljf
                        </p>
                    </blockquote>
                </div>
            </div>
            {{!-- {{/each}} --}}
        </div>
        <div class="col-md-3">
            <div class="card" style="width: 18rem;">
                <img src="{{imageUrl}}" class="card-img-top" alt="photo">
                <div class="card-body">
                    <h2 class="card-title">{{name}}</h2>
                    <hr>
                    <h4>{{eventName}}</h4>
                    <p class="card-text">{{description}}</p>
                    <p>{{date}}</p>
                    {{#each comments}}
                    {{description}}
                    <form action="/comentarios/borrar/{{id}}" method="POST" style="display:inline">
                        <input type="submit" value="Delete" class="btn btn-danger">
                    </form>
                    {{/each}}
                    <form action="/eventos/participar/{{id}}" method="POST">
                        <input type="submit" value="Participar" class="btn btn-outline-success">
                    </form>
                    <a href="/comentarios/crear/{{id}}">Crear comentario</a>
                    <hr>
                    <br>

                </div>

            </div>
        </div>